https://www.figma.com/design/TxnEoBDh2IPlFVsfDiktb4/Department-of-Animal-Production---Health?node-id=34-594&t=N3zA56OkWmf8Etf6-0

daph-ep-mis/                          ← Project root (upload to LGC / Docker)
├── index.php                         ← Front controller: Session check → Login or Dashboard redirect
├── logout.php                        ← Clears session, redirects to index.php
├── error.php                         ← 404/500 handler (custom for govt compliance)
├── .htaccess                         ← Clean URLs, block sensitive dirs (app/, storage/), force HTTPS
├── README.md                         ← Setup guide (LGC deploy, MySQLi config, 350 PD timeline)

├── config/
│   ├── db_connect.php                ← MySQLi connection ($mysqli = new mysqli(...); with error handling)
│   ├── constants.php                 ← Paths, SITE_NAME='DAPH Eastern Province', LANGUAGES=['en','si','ta']
│   ├── session_check.php             ← require_once; checks auth/role/office_id; redirects if invalid
│   └── permissions.php               ← Array of 10 roles × 60+ perms (e.g., $roles['pd']['diary_approve']=true;)

├── assets/                           ← Static files (Figma-optimized: govt blue #0033A0, white/grey accents)
│   ├── css/
│   │   ├── bootstrap.min.css         ← v5.x for responsive grids (Figma mobile frames)
│   │   ├── datatables.min.css        ← For report tables
│   │   ├── style.css                 ← Custom: Sinhala/Tamil font-faces (Noto Sans), RTL for Tamil, print styles for PDFs
│   │   └── dashboard.css             ← Cards/charts (Figma widget styles: shadows, hover effects)
│   ├── js/
│   │   ├── jquery.min.js             ← v3.x
│   │   ├── bootstrap.bundle.min.js
│   │   ├── datatables.min.js         ← With buttons for export (CSV/PDF/Excel as per SOW)
│   │   ├── chart.min.js              ← Chart.js for real-time dashboards (Figma has line/bar for project progress, pie for revenue)
│   │   └── custom.js                 ← AJAX for live updates (e.g., disease alerts), lang switch, e-sign canvas
│   ├── images/
│   │   └── logo.png                  ← DAPH/Sri Lanka govt emblem (from Figma assets)
│   └── fonts/                        ← (Optional) Local Sinhala/Tamil if no Google Fonts (Iskoola Pota.ttf)

├── includes/                         ← Reusable snippets (Figma layout: header w/ lang toggle, sidebar w/ role menus)
│   ├── header.php                    ← <html>, meta, CSS/JS includes, lang selector dropdown (SI/TA/EN)
│   ├── sidebar.php                   ← Dynamic menu via role (e.g., PD sees "Provincial Reports"; Vet sees "Treatments"); collapses on mobile
│   ├── footer.php                    ← Copyright, version, last updated (audit trail link)
│   └── functions.php                 ← Procedural helpers: format_date_np(), translate($key, $lang), validate_csrf(), log_activity($user_id, $action)

├── classes/                          ← Lightweight procedural wrappers (keeps code clean, not full OOP)
│   ├── DB.php                        ← MySQLi functions: db_query($sql, $params), db_escape($str), db_insert_id()
│   ├── Auth.php                      ← login_user($email, $pass), get_user_role(), is_allowed($module, $action)
│   └── Report.php                    ← generate_pdf($data, $type) using TCPDF (Figma PDF preview modal)

├── pages/                            ← Centralized views (Figma pages: each .php = one frame/screen)
│   ├── auth/
│   │   └── login.php                 ← Form + process (Figma login frame: centered card, govt branding)
│   ├── dashboard/                    ← 6 role-based (Figma has separate frames for each)
│   │   ├── provincial_director.php   ← Province-wide: Cards for total staff/revenue/projects + Chart.js (progress %)
│   │   ├── district.php              ← Filter by Amparai/Batti/Trinco; district metrics + sub-unit drill-down
│   │   ├── sms.php                   ← Epidemiology map/chart + supply chain alerts
│   │   ├── farms.php                 ← Poultry/dairy/fodder KPIs + revenue trends
│   │   ├── veterinary_office.php     ← Field-level: Daily diary, treatment queue, stock low alerts (18 activities from matrix)
│   │   └── admin_finance.php         ← HR/leave/projects/finance summaries + approval queues
│   ├── modules/                      ← Core functionality (Figma forms: inputs w/ validation, submit modals)
│   │   ├── hr/
│   │   │   ├── leave_apply.php       ← Form for staff leave (Figma: calendar picker, approval workflow)
│   │   │   ├── leave_approve.php     ← PD/District table w/ approve/reject + e-sign button
│   │   │   ├── staff_attendance.php  ← Daily punch-in/out + reports
│   │   │   └── diary_advance_programme.php ← Diary entry + advance to-do list (multi-day, attach files)
│   │   ├── finance/
│   │   │   ├── assets_management.php   
│   │   │   ├── procurement_plan.php  
│   │   │   ├── finance_disbursementsources.php
│   │   │   ├── veterinary_stores.php   
│   │   |
│   │   ├── veterinary
│   │   │   ├── animal_treatment.php  ← Indoor/outdoor forms (species, symptoms, meds; Figma multi-step wizard)
│   │   │   ├── disease_reporting.php ← Real-time alerts + provincial epi dashboard integration
│   │   │   ├── ai_castration.php     ← AI/embryo/castration logs + materials distribution
│   │   │   ├── stock_management.php  ← Drugs/vaccines/semen inventory + low-stock notifications
│   │   │   └── farmer_registration.php ← DB for farms/farmers + dairy hubs
│   │   ├── farm/
│   │   │   ├── poultry_hatchery.php  ← Operations log + revenue
│   │   │   ├── livestock_operations.php  ← Milk yield/market price reporting
│   │   │   └── fodder_distribution.php ← Pasture materials supply + farmer training links
|   |   |   ├── inputs_revenue.php
│   │   ├── project/
│   │   │   ├── psdg_projects.php     ← PSDG/CBG/NGO listing + add new
│   │   │   └── progress_physical_financial.php ← Forms/charts for % complete (Figma bar/line graphs)
│   │   └── training/
│   │   │   └── training_activities.php   ← Schedule/attendance/revenue
|   |   │    └── farmer_participation.php
|   |   │    └── revenue_management.php
│   │   └── sms/
│   │   │    ├── my_diary.php                  ← Diary entry & advance programme
│   │   │    ├── supply_stock_alerts.php       ← Low stock alerts + supply chain overview
│   │   │    ├── provincial_epidemiology.php   ← Provincial epi dashboard + map/chart
│   │   │    └── veterinary_supply_chain.php   ← Full supply chain management
│   │   ├── district/
│   │   │   ├── Diary Management.php   
│   │   │   ├── Revenue Management.php  
│   │
│   │
│   │──── reports/
│           ├── generate_pdf.php          ← AJAX endpoint: Calls Report.php class for status/risk/resource reports (SOW reqs)
│           ├── provincial_summary.php    ← Consolidated view (Figma: tabbed, export buttons)
│           └── district_dashboard.php    ← Per-district drill-down (links to role dashboards)
│
├── auth/                             ← Dedicated auth (separate from pages/ for security)
│   ├── login_process.php             ← Handles POST, sets $_SESSION['user_id', 'role', 'office_id', 'lang']
│   └── change_password.php           ← Secure reset (email link or admin-only)

├── admin/                            ← Super-admin only (Figma admin frame: user table w/ search/export)
│   ├── manage_users.php              ← CRUD for 130–220 users (import CSV for 45 offices)
│   ├── role_permissions.php          ← Matrix editor (10 roles: PD, District DD, SMS, Farms DD, VS, LDO, Admin, etc.)
│   └── system_logs.php               ← Audit trails (who/what/when; searchable, exportable for Electronic Transactions Act)

├── uploads/                          ← Private (block via .htaccess; Figma: secure upload zones)
│   ├── signatures/                   ← User e-sign images/PNG (canvas draw + upload)
│   ├── reports/                      ← Generated PDFs (timestamped, access-logged)
│   └── documents/                    ← Attached files (diaries, approvals; virus-scan hook if needed)

├── lang/                             ← Trilingual arrays (Figma: dynamic text swap via JS/PHP)
│   ├── en.php                        ← $lang['dashboard_title'] = 'Dashboard';
│   ├── si.php                        ← Sinhala Unicode: $lang['dashboard_title'] = 'බලපත්‍රය';
│   └── ta.php                        ← Tamil Unicode: $lang['dashboard_title'] = 'டாஷ்போர்டு';

├── storage/                          ← Private backups/logs (outside web root for LGC)
│   ├── logs/                         ← activity.log, error.log (rotate monthly)
│   └── backups/                      ← DB dumps (cron job)
└── vendor/                           ← (Optional) TCPDF/FPDF here if not classes/ (download manually)